{
  "name": "zivo-nextjs-developer",
  "version": "1.0.0",
  "private": true,
  "description": "E2B sandbox template Zivo builds apps in. `name` is the template alias — namespaced so it cannot resolve to E2B's public `nextjs-developer` template.",
  "scripts": {
    "build": "bun run ./build.prod.ts",
    "build:dev": "bun run ./build.dev.ts"
  }
}

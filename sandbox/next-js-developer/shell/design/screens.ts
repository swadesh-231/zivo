import type { ScreenDef } from "./types";

/**
 * Replaced by the design agent, in the order the screens should be read.
 *
 * Each entry points at a component in `app/screens/`. Nothing else in the
 * shell needs editing to add a screen.
 */
export const SCREENS: ScreenDef[] = [];
